export type PokemonListItem = {
  readonly name: string;
  readonly url: string;
  jpName: string;
};

export type PokemonFeature = {
  baseHappiness: number;
  captureRate: number;
  color: string;
};
